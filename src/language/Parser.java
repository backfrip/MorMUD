package language;

import java.util.Arrays;

public abstract class Parser {
    public static void parseConsole(String s) {
	s.trim();
	if (s.equals(""))
	    return;
	String[] c = 
	System.out.println("Parsed:" + Arrays.toString(c));
    }
}
